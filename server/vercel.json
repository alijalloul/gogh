{
  "version": 2,
  "builds": [{
      "src": "src/main.js",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run postinstall"
      }
  }],
  "routes": [
      {
          "src": "/(.*)",
          "dest": "src/main.js",
          "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
          "headers": {
                "Access-Control-Allow-Origin": "*"
            }
      }
  ]
}
